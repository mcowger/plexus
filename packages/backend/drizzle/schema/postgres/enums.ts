// OAuth provider IDs and quota checker types are intentionally stored as
// free-text columns. Both are validated dynamically at the application layer
// because their registered values can change without a database migration.
