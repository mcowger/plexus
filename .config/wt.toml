# .config/wt.toml — checked into the repo, shared with all contributors

# --- new worktree setup ---

[[pre-start]]
trust = "mise trust"

[[pre-start]]
install = "bun install"

[[pre-start]]
build = "bun run build:frontend"

# --- after merge: push to origin automatically ---

[post-merge]
push = "git push origin {{ target }}"
